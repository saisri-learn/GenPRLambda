{
  "name": "genpr-lambda",
  "version": "0.1.0",
  "description": "REPO MCP server for Lambda function",
  "private": true,
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "@modelcontextprotocol/server-REPO": "^0.5.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
